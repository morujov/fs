<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Драйвер
    |--------------------------------------------------------------------------
    | log        — коды пишутся в storage/logs/laravel.log, SMS не уходят
    | labsmobile — боевой, ~0.05 €/SMS
    |
    | По умолчанию log: цикл отладки формы подачи — это десятки отправок
    | в день, и боевой драйвер, включённый «на минутку», съедает бюджет.
    | SmsManager не даст использовать log в продакшене.
    */

    'driver' => env('SMS_DRIVER', 'log'),

    // Отправитель (TPOA). Испания разрешает буквенный, до 11 символов.
    'from' => env('SMS_FROM', 'NumerosES'),

    'labsmobile' => [
        'username' => env('LABSMOBILE_USERNAME'),
        'token'    => env('LABSMOBILE_TOKEN'),
    ],

];
