<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Блок-лист намеренно пуст.
 *
 * ── Почему ─────────────────────────────────────────────────────────────
 * Блок-лист — инструмент администратора для точечных запретов, которые
 * выясняются в работе: номер организации, выставленный шутником; номер из
 * подтверждённой жалобы «это мой номер»; номер, вокруг которого крутится
 * мошенничество. Всё это узнаётся постфактум и добавляется из админки.
 *
 * Системные правила нумерации здесь НЕ живут — они в `numbering_ranges`:
 * 70X (numeración personal), 8XX/9XX (фиксированная связь), 900/902
 * (служебные) — это план нумерации, а не блок-лист. Держать правило в двух
 * местах значит однажды поправить одно и забыть другое.
 *
 * ── Чем был занят раньше ───────────────────────────────────────────────
 * Здесь лежали выдуманные записи: '666666666' с пометкой «проверить
 * вручную» и '612345678' как «пример из документации». Обе оказались
 * вредны. 666666666 — самый ценный repetido в стране, запрещать его
 * бессмысленно. 612345678 — это escalera. Фабрика демо-данных честно
 * генерировала оба, и данные противоречили собственному блок-листу.
 * Пустой блок-лист правдивее выдуманного.
 */
class BlocklistSeeder extends Seeder
{
    public function run(): void
    {
        // Ничего не сеем. Записи добавляет администратор через админку
        // либо конвейер модерации по подтверждённой жалобе.
    }
}
